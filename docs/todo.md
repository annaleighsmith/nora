## CLI design

- `-q` for ask is weird — find a better short flag
- Consider renaming `look` to `show`, `cat`, or `view`
- Move `health` back to a subcommand instead of a global flag
- Add `-s` short flag for search
